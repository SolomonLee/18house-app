const Split = (props) => {
  return (
    <div className="split_item">
      <div className="item_content">
        <span>{props.content}</span>
      </div>
    </div>
  );
};

export default Split;
